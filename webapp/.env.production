# Loaded by `next build` / production runtime only (NOT local `next dev`).
# Vercel dashboard env vars still take precedence, so this only ADDS the flag.
# Enables the cosmetic @Juan demo profile on the deployed site.
NEXT_PUBLIC_DEMO_MODE=true
